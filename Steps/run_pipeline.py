from pipeline.training_pipeline import train_pipeline  # Correct the import path

if __name__ == '__main__':  # Correct the `__name__` block
    # Run the pipeline
    train_pipeline(data_path=r'E:\Projects\MLOps\data\olist_customers_dataset.csv')
